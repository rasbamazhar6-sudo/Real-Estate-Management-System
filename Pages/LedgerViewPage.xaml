<Page x:Class="Project.Pages.LedgerViewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BackgroundColor}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <Grid Margin="32">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <!-- PAGE HEADER -->

            <StackPanel Grid.Row="0" Margin="0,0,0,28">

                <TextBlock Text="Ledger View"
                           Style="{StaticResource PageHeaderStyle}"/>

                <TextBlock Text="View detailed debit and credit transactions, balances, and aging for a selected customer"
                           FontSize="15"
                           Foreground="{StaticResource TextSecondaryColor}"/>

            </StackPanel>


            <!-- CUSTOMER SELECTION -->

            <Border Grid.Row="1"
                    Style="{StaticResource CardStyle}"
                    Margin="0,0,0,24">

                <Grid Margin="24">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>

                        <TextBlock Text="Select Customer"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>

                        <ComboBox x:Name="cmbSelectCustomer"
                                  Style="{StaticResource ModernComboBoxStyle}"
                                  Height="38"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="CustomerId"
                                  SelectionChanged="CmbSelectCustomer_SelectionChanged"/>

                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="Link to Plots / Projects"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Height="38"
                            Margin="16,24,0,0"
                            Click="BtnLinkToPlots_Click"/>

                </Grid>

            </Border>


            <!-- LEDGER CARD -->

            <Border Grid.Row="2"
                    Style="{StaticResource CardStyle}">

                <Grid Margin="24">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <!-- SUMMARY -->

                    <Grid Grid.Row="0" Margin="0,0,0,24">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>


                        <!-- Running Balance -->

                        <Border Grid.Column="0"
                                Background="#E3F2FD"
                                CornerRadius="8"
                                Padding="20">

                            <StackPanel>

                                <TextBlock Text="Running Balance"
                                           FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>

                                <TextBlock x:Name="txtRunningBalance"
                                           Text="PKR 0.00"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryColor}"/>

                            </StackPanel>

                        </Border>


                        <!-- Outstanding -->

                        <Border Grid.Column="2"
                                Background="#FFF3E0"
                                CornerRadius="8"
                                Padding="20">

                            <StackPanel>

                                <TextBlock Text="Outstanding Amount"
                                           FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>

                                <TextBlock x:Name="txtOutstandingAmount"
                                           Text="PKR 0.00"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource WarningColor}"/>

                            </StackPanel>

                        </Border>


                        <!-- Total Credit -->

                        <Border Grid.Column="4"
                                Background="#E8F5E9"
                                CornerRadius="8"
                                Padding="20">

                            <StackPanel>

                                <TextBlock Text="Total Credit"
                                           FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>

                                <TextBlock x:Name="txtTotalCredit"
                                           Text="PKR 0.00"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource SuccessColor}"/>

                            </StackPanel>

                        </Border>

                    </Grid>


                    <!-- LEDGER TITLE -->

                    <TextBlock Grid.Row="1"
                               Text="Customer Transaction History"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,16"/>


                    <!-- LEDGER GRID -->

                    <DataGrid x:Name="dgLedger"
                              Grid.Row="2"
                              Style="{StaticResource ModernDataGridStyle}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionChanged="DgLedger_SelectionChanged">

                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Date"
                                                Binding="{Binding Date, StringFormat=dd/MM/yyyy}"
                                                Width="120"/>

                            <DataGridTextColumn Header="Description"
                                                Binding="{Binding Description}"
                                                Width="*"/>

                            <DataGridTextColumn Header="Reference"
                                                Binding="{Binding Reference}"
                                                Width="160"/>

                            <DataGridTextColumn Header="Debit"
                                                Binding="{Binding Debit, StringFormat='PKR {0:N2}'}"
                                                Width="140">

                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource ErrorColor}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>

                            </DataGridTextColumn>


                            <DataGridTextColumn Header="Credit"
                                                Binding="{Binding Credit, StringFormat='PKR {0:N2}'}"
                                                Width="140">

                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>

                            </DataGridTextColumn>


                            <DataGridTextColumn Header="Balance"
                                                Binding="{Binding Balance, StringFormat='PKR {0:N2}'}"
                                                Width="150">

                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>

                            </DataGridTextColumn>


                            <DataGridTextColumn Header="Aging"
                                                Binding="{Binding Aging}"
                                                Width="120"/>

                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>

            </Border>

        </Grid>

    </ScrollViewer>

</Page>